use hubproxy::hubproxy;

struct HubConnection {
    url : String

}

impl HubConnection {
    fn new (url : String) -> Self {
        HubConnection {
            url : String
        }
    }

    fn create_hub_proxy (proxy : &str) -> HubProxy {

    }
}
